import 'package:flutter/material.dart';


const bgSecondaryColor = Color.fromRGBO(248, 249, 250, 1);
const bgPrimaryColor = Colors.white;
const greyColor = Color.fromRGBO(228, 228, 228, 1);
const accentColor = Color.fromRGBO(245, 159, 2, 1);
const textColor = Color.fromRGBO(109, 109, 109, 1);

final List<BoxShadow> shadow = <BoxShadow>[
  const BoxShadow(
    color: Colors.black12,
    offset: Offset(0.0, 3.0),
    blurRadius: 12.0,
    spreadRadius: 1.0,
  ),
];

final BorderRadius borderRadius = BorderRadius.circular(6);
